<UserControl x:Class="BuildSys.Views.CustomerManage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BuildSys.Views"
             xmlns:models="clr-namespace:BuildSys.Models"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <!--<DataGrid Margin="20 20 20 20"  ItemsSource="{Binding customers}"/>-->
        <DataGrid Margin="20 20 20 20"  ItemsSource="{Binding customers}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding editCustomer}">Edit Customer</Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Binding="{Binding firstname}" Header="Firstname" />
                <DataGridTextColumn Binding="{Binding surname}" Header="Surname" />
                <DataGridTextColumn Binding="{Binding semail}" Header="Email" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
