<UserControl x:Class="BuildSys.Views.CustomerList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BuildSys.Views"
             xmlns:models="clr-namespace:BuildSys.Models"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <!--
    <UserControl.Resources>
        <ObjectDataProvider x:Key="CustomerModel"
           ObjectType="{x:Type models:CustomerModel}"/>
        <ObjectDataProvider x:Key="Customers" MethodName="GetCustomers"
           ObjectInstance="{StaticResource CustomerModel}" />
    </UserControl.Resources>
    -->
    <Grid>
        <DataGrid Margin="20 20 20 20"  ItemsSource="{Binding Customers}" />
    </Grid>
</UserControl>
