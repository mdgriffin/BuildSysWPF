<UserControl x:Class="BuildSys.CustomerList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BuildSys"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <!-- the customers datasource -->
        <ObjectDataProvider x:Key="CustomerModel"
           ObjectType="{x:Type local:CustomerModel}"/>
        <ObjectDataProvider x:Key="Customers" MethodName="GetCustomers"
           ObjectInstance="{StaticResource CustomerModel}" />
    </UserControl.Resources>
    
    <Grid>
        <DataGrid x:Name="dataGrid" ItemsSource="{Binding Customers}" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top"/>

    </Grid>
</UserControl>
